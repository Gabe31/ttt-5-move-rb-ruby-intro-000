#!/usr/bin/env ruby

require_relative '../lib/move.rb'

puts "Welcome to Tic Tac Toe!"

board = [" ", " ", " ", " ", " ", " ", " ", " ", " "]

puts "Where would you like to go?"
gets.strip(input)

input_to_index(input) = index

move(board, index, "X")

updated_board = move(board, index, "X")

puts display_board(updated_board)
